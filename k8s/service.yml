apiVersion: v1
kind: Service
metadata:
  name: devops-project-app
  labels:
    app: devops-project-app
spec:
  type: ClusterIP
  selector:
    app: devops-project-app
  ports:
    - name: http
      port: 80
      targetPort: 5001
      protocol: TCP